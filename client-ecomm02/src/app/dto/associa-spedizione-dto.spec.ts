import { AssociaSpedizioneDto } from './associa-spedizione-dto';

describe('AssociaSpedizioneDto', () => {
  it('should create an instance', () => {
    expect(new AssociaSpedizioneDto()).toBeTruthy();
  });
});
